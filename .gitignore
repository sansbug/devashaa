# Python
.venv/
__pycache__/
*.pyc

# Node / Vite
node_modules/
dist/

# Local env (keep the .example files)
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# NOTE: api/ephe/*.se1 ARE committed on purpose — the backend Docker image needs
# them, and they are redistributable with Swiss Ephemeris. ~1.9 MB total.

# Wrangler local dev state — miniflare's simulated KV/cache and bundle temp
# files. Machine-local build artifacts; a local KV database in a public repo is
# noise at best.
web/.wrangler/
