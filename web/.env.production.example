# Copy these into Cloudflare Pages → Settings → Environment variables (Production).
# VITE_API_URL is your Render backend URL (no trailing slash).
VITE_API_URL=https://devashaa-api.onrender.com
# Link to your published source, for AGPL compliance (Swiss Ephemeris).
VITE_SOURCE_URL=https://github.com/YOURNAME/devashaa
